# Synthetic AgentLedger demo data

This directory is a deliberately fictional, self-contained audit input for
`ledger --demo report`. It does not represent a real company, repository, or
incident history. It contains one high-risk authentication change linked to a
synthetic incident and one low-risk documentation change, so the HTML and
terminal reports visibly demonstrate both review states without Git access or
an OpenAI API key.
